package catan;

/**
 * Enumeration of building types that can exist on a node.
 * NONE means no building, SETTLEMENT is worth 1 VP, CITY is worth 2 VP.
 */
public enum BuildingType {
    NONE,
    SETTLEMENT,
    CITY
}

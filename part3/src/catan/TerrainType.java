package catan;

/**
 * Enumeration of terrain types for hex tiles on the Catan board.
 * Each terrain type (except DESERT) produces a corresponding resource.
 */
public enum TerrainType {
    WOOD,
    BRICK,
    SHEEP,
    WHEAT,
    ORE,
    DESERT
}

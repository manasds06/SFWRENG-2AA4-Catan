package catan;

/**
 * Enumeration of resource types that players can collect and spend.
 * Maps directly to terrain types (except DESERT produces no resource).
 */
public enum ResourceType {
    WOOD,
    BRICK,
    SHEEP,
    WHEAT,
    ORE
}

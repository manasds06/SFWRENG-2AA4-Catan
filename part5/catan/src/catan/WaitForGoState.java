package catan;

public class WaitForGoState implements TurnState {

	@Override
	public void handleTurn(CatanSimulator context, Agent a) {
		// TODO: idle/paused state — wait for external signal (e.g. stepForward) to proceed
	}
}

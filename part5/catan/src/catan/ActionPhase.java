package catan;

public class ActionPhase implements TurnState {

	@Override
	public void handleTurn(CatanSimulator context, Agent a) {
		// TODO: ask agent to choose action, execute it, then transition to WaitForGoState or next turn
	}
}
